import 'package:flutter/material.dart';

class Account extends StatelessWidget {
  @override
  Widget build(BuildContext context) => _Account();
  }

class _Account extends Account {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Text("coucou"),
    );
  }
}
